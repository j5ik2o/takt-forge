与えられたタスクの説明から、EARS形式の要件ドキュメントを生成せよ。

**注意:** 既存の仕様ディレクトリが存在する場合は、既存の要件を上書きせず追加・更新する。

**やること:**
1. タスクから対象feature名を特定する
2. `.kiro/steering/` ディレクトリが存在すれば全ファイルを読み込み、プロジェクトコンテキストを把握する
3. `.kiro/specs/` 配下に対象featureのディレクトリが存在するか確認する
4. タスクの説明を分析し、機能要件・非機能要件を抽出する
5. 各要件にEARS形式の受け入れ条件を作成する
6. 正常系・異常系・境界条件を網羅する
7. 要件間の依存関係を特定する
8. 成果物を `.kiro/specs/{feature}/requirements.md` に保存する

**成果物の保存先:**
- ディレクトリ: `.kiro/specs/{feature}/`（存在しなければ作成）
- ファイル: `requirements.md`

**必須出力（見出しを含める）**

## 導入
- 機能の概要と目的を1-2段落で記述

## 要件
- 要件ごとにセクションを分ける
- 各要件に目的文と受け入れ条件を含める
- 要件IDは数値のみ（1, 1.1, 2, 2.1 ...）
